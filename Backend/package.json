{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  

  
"scripts": {
    "test": "cross-env NODE_ENV=test mocha --timeout 30000 \"tests/**/*.test.js\"",
    "coverage": "cross-env NODE_ENV=test nyc --reporter=text --reporter=text-summary --reporter=lcov mocha --timeout 30000 \"tests/**/*.test.js\"",
    "coverage:json": "cross-env NODE_ENV=test nyc --reporter=json --reporter=json-summary mocha --timeout 30000 --reporter json \"tests/**/*.test.js\" > mocha-results.json",
    "dev": "nodemon index.js",
    "start": "node index.js"
  },

  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.1",
    "helmet": "^8.1.0",
    "joi": "^18.0.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.17.1",
    "pino": "^9.8.0",
    "pino-http": "^10.5.0",
    "sanitize-html": "^2.17.0"
  },
  "devDependencies": {
    "chai": "^5.2.1",
    "cross-env": "^10.0.0",
    "mocha": "^11.7.1",
    "mongodb-memory-server": "^10.2.0",
    "nodemon": "^3.1.10",
    "nyc": "^17.1.0",
    "pino-pretty": "^13.1.1",
    "supertest": "^7.1.4"
  }
}
